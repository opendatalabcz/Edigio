import { MultilingualTextTranslatePipe } from './multilingual-text-translate.pipe';

describe('MultilingualTextTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new MultilingualTextTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
