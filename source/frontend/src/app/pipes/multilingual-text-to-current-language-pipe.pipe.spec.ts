import { MultilingualTextToCurrentLanguagePipe } from './multilingual-text-to-current-language.pipe';

describe('MultilingualTextToCurrentLanguagePipePipe', () => {
  it('create an instance', () => {
    const pipe = new MultilingualTextToCurrentLanguagePipe();
    expect(pipe).toBeTruthy();
  });
});
