/**
 * Check whether value is defined and not blank
 *
 * @param value Value to be checked
 *
 * @return true when string is defined and not blank, false otherwise
 */
export function isDefinedNotBlank(value?: string) : boolean {
  return !!value
}
