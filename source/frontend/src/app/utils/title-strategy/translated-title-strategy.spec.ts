import { TranslatedTitleStrategy } from './translated-title-strategy';

describe('TranslatedTitleStrategy', () => {
  it('should create an instance', () => {
    expect(new TranslatedTitleStrategy()).toBeTruthy();
  });
});
